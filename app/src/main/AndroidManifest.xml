<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.abed.e_comkotlin">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EcomKotlin"
        android:usesCleartextTraffic="true">
        <activity android:name=".Activity.Cartshow"></activity>
        <activity android:name=".Activity.ExploreData" />
        <activity android:name=".Activity.exploreActivity" />
        <activity android:name=".Activity.SqlCrud" />
        <activity
            android:name=".Activity.homeScreen"
            android:label="@string/title_activity_home_screen"
            android:theme="@style/Theme.EcomKotlin.NoActionBar" />
        <activity android:name=".Activity.login_screen" />
        <activity android:name=".Activity.createAccountInformation" />

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/actions" />

        <activity android:name=".Activity.createAccountScreen" />
        <activity android:name=".Activity.splashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>